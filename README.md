
  # one-line art

  This is a code bundle for one-line art. The original project is available at https://www.figma.com/design/hRWcto0PIn5P1UAnLN2aGL/one-line-art.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  